



function solve(billValue):{      // fucntion called solve - purpose is to take billvalue from input of user, and use to calculate two tip options anc choose the bigger value.

    if (the button is clicked) and (billValue has a value):
        
        let billValue = document.getElementById("myInputBox").value;
        
        if (button id = tip1) 
            x = 5%
        elif (button id = tip2)
            X = 10%
        else:
            x = 25%
    
        z = x*billValue
        za = (x*billValue) +2

        if z > za:
            result = z
        else:
            result = za

        return result
    else:
        // send error message?
}